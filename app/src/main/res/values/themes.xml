<!-- No arquivo: res/values/themes.xml -->
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema base do aplicativo. Herda DIRETAMENTE de um tema Light. -->
    <style name="Theme.MatrixLab" parent="Theme.Material3.Light.NoActionBar">
        <!-- Suas cores primárias, secundárias, etc. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
    </style>

    <!-- 💡 CORREÇÃO: Adicionando os estilos que faltavam de volta -->
    <style name="Theme.MatrixLab.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.MatrixLab.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>